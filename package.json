{
  "name": "fletes-ostrit",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix fletes-driver-pwa run build",
    "test:api": "node --test api"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "engines": {
    "node": "20.x"
  }
}
